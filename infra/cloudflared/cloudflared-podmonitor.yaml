apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cloudflared
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: cloudflared
  namespaceSelector:
    matchNames:
      - cloudflared
  podMetricsEndpoints:
    - port: metrics
      scheme: http
      path: /metrics
