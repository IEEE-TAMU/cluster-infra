apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: email
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: kumo
  namespaceSelector:
    matchNames:
      - email
  podMetricsEndpoints:
    - port: metrics
      scheme: http
      path: /metrics
